module.exports = {
  env: {
    mocha: true,
  },
  globals: {
    expect: true,
    should: true,
    assert: true,
    needs: true,
  },
  rules: {
    'no-unused-expressions': `off`,
    'no-unused-vars': `off`,
  },
};
