/**
 * TEMPORARILY DISABLED FILE
 */
// import { default as mongoose } from "mongoose";

// import Token from "./token.js";

// const sessionSchema = mongoose.Schema({
//   name: {
//     type: String,
//     required: true,
//     default: () => `Session ` + `${Math.random()}`.substring(2),
//   },
//   active: {
//     type: Token,
//   },
// });

export default null;
